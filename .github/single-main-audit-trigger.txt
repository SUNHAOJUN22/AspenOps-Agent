This transient file exists only to trigger the corrected, observable single-main governance audit.
The audit waits for Linux CI and Windows control-plane success, closes this PR, archives and deletes this branch, removes the one-shot workflow, and publishes docs/single-main-audit.* on main.
