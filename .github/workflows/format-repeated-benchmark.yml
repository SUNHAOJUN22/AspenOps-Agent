name: One-shot format repeated benchmark files

on:
  push:
    branches: [agent/aspenops-v2-reliability-performance]
    paths:
      - .github/workflows/format-repeated-benchmark.yml

permissions:
  contents: write

concurrency:
  group: aspenops-one-shot-format-repeated-benchmark
  cancel-in-progress: false

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/aspenops-v2-reliability-performance
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - name: Sync locked development dependencies
        run: uv sync --extra dev
      - name: Apply repository formatter
        run: |
          uv run ruff format scripts/run_benchmark_matrix.py \
            tests/test_benchmark_repeated_trials.py
          uv run ruff check scripts/run_benchmark_matrix.py \
            tests/test_benchmark_repeated_trials.py
      - name: Commit formatting and remove one-shot workflow
        shell: bash
        run: |
          rm .github/workflows/format-repeated-benchmark.yml
          git config user.name "aspenops-ci"
          git config user.email "aspenops-ci@users.noreply.github.com"
          git add scripts/run_benchmark_matrix.py \
            tests/test_benchmark_repeated_trials.py \
            .github/workflows/format-repeated-benchmark.yml
          git commit -m "style: format repeated benchmark implementation"
          git push origin HEAD:agent/aspenops-v2-reliability-performance
